<h1>Books</h1>

<% @books.each do |book| %>
  <div class = "box">
  <h1><%= link_to book.title, book %></h1>
  <%= image_tag book.image, :class => 'thumb' if book.image.present? %>
  </div>
<% end %>
